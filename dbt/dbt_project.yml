name: 'NYC_Taxi_Pipeline'
version:  '1.0.0'
profile: 'NYC_Taxi_Pipeline'

require-dbt-version: '>=1.0.4'

model-paths: ['models']
seed-paths: ['seeds']

models:
  NYC_Taxi_Pipeline:
    bronze:
      +materialized: view
      +schema: bronze
    silver:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold

seeds:
  NYC_Taxi_Pipeline:
    +schema: seeds
